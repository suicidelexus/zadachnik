# Self's Product Board

Трекер задач для управления продуктом с поддержкой Kanban, Priority Score (VRC) и Матрицы Эйзенхауэра.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Запуск приложения

```bash
python main.py
```

Приложение будет доступно по адресу: `http://127.0.0.1:5000`

## 📦 Синхронизация с GitHub

📘 **[Подробное руководство по настройке GitHub](GITHUB_SETUP_GUIDE.md)**  
🔄 **[Руководство по автоматической синхронизации](AUTO_SYNC_GUIDE.md)**

Для работы с проектом на разных компьютерах используйте автоматическую синхронизацию:

### ⚡ Быстрая синхронизация (рекомендуется)

```bash
./s
```

Эта команда:
1. Получает последние изменения с GitHub
2. Добавляет ваши изменения
3. Делает коммит
4. Отправляет на GitHub

### 🤖 Автоматическая синхронизация в фоне

Для непрерывной автоматической синхронизации:

```bash
python auto_sync.py
```

Скрипт будет автоматически синхронизировать изменения каждую минуту.

### Способ 1: Bash скрипт (рекомендуется)

```bash
./sync_github.sh
```

### Способ 2: Python скрипт

```bash
python sync_github.py
```

Или с кастомным сообщением коммита:

```bash
python sync_github.py "Описание изменений"
```

### Способ 3: Вручную

```bash
git add .
git commit -m "Описание изменений"
git push origin main
```

## 🔄 Работа на другом компьютере

### Первый раз (клонирование репозитория)

```bash
git clone https://github.com/suicidelexus/zadachnik.git
cd zadachnik
pip install -r requirements.txt
python main.py
```

### Получение последних изменений

```bash
git pull origin main
```

## 📋 Основные функции

- **Канбан доска** - визуальное управление задачами по статусам
- **Table View** - табличный режим для быстрого просмотра
- **Priority Score (VRC)** - расчёт приоритета задач на основе Value, Reach, Budget Impact и Confidence
- **Матрица Эйзенхауэра** - классификация задач по важности и срочности
- **Дашборд** - аналитика и статистика по задачам
- **Импорт/Экспорт** - поддержка CSV и Excel файлов
- **Теги и группы** - организация задач
- **Тёмная и светлая темы** - комфортная работа в любое время
- **Управление пользователями** - назначение постановщиков и исполнителей

## 🗂 Структура проекта

```
zadachnik/
├── main.py                 # Главный файл приложения
├── models.py              # Модели данных (SQLAlchemy)
├── requirements.txt       # Зависимости Python
├── routes/               # Маршруты Flask
│   ├── dashboard.py
│   ├── tasks.py
│   ├── rice.py
│   └── ...
├── templates/            # HTML шаблоны
│   ├── base.html
│   ├── kanban.html
│   └── ...
├── static/              # Статические файлы
│   ├── css/
│   └── js/
└── instance/            # База данных SQLite (не в Git)
```

## 🛠 Технологии

- **Backend**: Flask (Python)
- **Database**: SQLite
- **Frontend**: HTML5, CSS3, Vanilla JavaScript
- **UI Framework**: DaisyUI + Tailwind CSS
- **Icons**: Material Design Icons

## 📝 Рабочий процесс с Git

### Перед началом работы

```bash
git pull origin main
```

### После внесения изменений

```bash
./sync_github.sh
# или
python sync_github.py
```

### Разрешение конфликтов

Если возникли конфликты при pull:

```bash
git stash                    # Сохранить локальные изменения
git pull origin main         # Получить изменения с GitHub
git stash pop               # Применить локальные изменения
# Разрешите конфликты вручную
git add .
git commit -m "Разрешение конфликтов"
git push origin main
```

## 🎨 Особенности UI

- Адаптивный дизайн
- Glassmorphism эффекты
- Плавные анимации и переходы
- Компактное расположение элементов
- Цветовое кодирование приоритетов и статусов

## 📄 Дополнительные документы

- `test_functionality.md` - Руководство по тестированию
- `INTERFACE_IMPROVEMENTS.md` - История улучшений интерфейса
- `DARK_THEME_IMPROVEMENTS.md` - Улучшения тёмной темы
- `UI_COMPONENTS_GUIDE.md` - Руководство по UI компонентам

## ⚙️ База данных

База данных SQLite находится в папке `instance/` и **НЕ синхронизируется** с GitHub по умолчанию.

Если нужно синхронизировать БД между компьютерами, раскомментируйте соответствующие строки в `.gitignore`.

**⚠️ Внимание**: Синхронизация БД через Git может привести к конфликтам. Рекомендуется использовать экспорт/импорт через интерфейс приложения.

## 🔐 Безопасность

- Не коммитьте файлы `.env` с секретными данными
- Используйте `.gitignore` для исключения чувствительной информации
- Регулярно делайте резервные копии базы данных

## 🤝 Contributing

Проект создан для личного использования командой из 4 человек для поддержания душевного баланса и равновесия 🧘

---

Made with ❤️ for Self's Product Board

