# 📘 Руководство по настройке GitHub для zadachnik

## ✅ Что уже сделано

1. ✅ Git репозиторий инициализирован
2. ✅ Все файлы добавлены в коммит
3. ✅ Создан первый коммит
4. ✅ Настроен remote origin: https://github.com/suicidelexus/zadachnik.git
5. ✅ Созданы скрипты для автоматизации: `sync_github.sh` и `sync_github.py`
6. ✅ Создан `.gitignore` для исключения ненужных файлов

## 🔐 Что нужно сделать вручную

### Шаг 1: Получите Personal Access Token

Для push в GitHub требуется Personal Access Token (не обычный пароль).

**Инструкция:**

1. Откройте в браузере: https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Заполните форму:
   - **Note**: `zadachnik` (название для вашей памяти)
   - **Expiration**: выберите `No expiration` или `90 days`
   - **Select scopes**: отметьте галочкой **`repo`** (это даст полный доступ к репозиториям)
4. Нажмите **"Generate token"** внизу страницы
5. **ВАЖНО**: Скопируйте токен в безопасное место (он покажется только один раз!)
   - Токен выглядит примерно так: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

### Шаг 2: Проверьте, существует ли репозиторий на GitHub

1. Откройте в браузере: https://github.com/suicidelexus/zadachnik
2. Если репозиторий не существует:
   - Перейдите на https://github.com/new
   - **Repository name**: `zadachnik`
   - **Description** (опционально): "Self's Product Board - таск-трекер для управления продуктом"
   - Выберите **Public** или **Private** (на ваш выбор)
   - **НЕ добавляйте** README, .gitignore или license (они уже есть локально)
   - Нажмите **"Create repository"**

### Шаг 3: Выполните push в GitHub

Откройте терминал и выполните:

```bash
cd /Users/hybrid/PycharmProjects/zadachnik
git push -u origin main
```

**Система запросит:**
- **Username for 'https://github.com'**: введите `suicidelexus`
- **Password for 'https://suicidelexus@github.com'**: вставьте ваш **Personal Access Token**

После успешного ввода токена, все файлы будут отправлены на GitHub!

### Шаг 4: Настройте сохранение credentials (опционально)

Чтобы не вводить токен каждый раз:

```bash
# Для macOS (использует Keychain):
git config --global credential.helper osxkeychain

# Для Linux:
git config --global credential.helper store
```

После этой настройки токен сохранится автоматически при первом успешном push.

## 🚀 Использование после настройки

### Автоматическая синхронизация (рекомендуется)

После каждого изменения используйте скрипт:

```bash
# Вариант 1 (bash):
./sync_github.sh

# Вариант 2 (python с кастомным сообщением):
python sync_github.py "Добавил новую фичу"

# Вариант 3 (python с авто-сообщением):
python sync_github.py
```

### Ручная синхронизация

```bash
git add .
git commit -m "Описание изменений"
git push origin main
```

## 💻 Работа на другом компьютере

### Первый раз (клонирование)

```bash
git clone https://github.com/suicidelexus/zadachnik.git
cd zadachnik
pip install -r requirements.txt
python main.py
```

При клонировании система также попросит:
- Username: `suicidelexus`
- Password: ваш Personal Access Token

### Получение последних изменений

Перед началом работы на другом компьютере:

```bash
cd zadachnik
git pull origin main
```

### Отправка изменений

После работы:

```bash
cd zadachnik
./sync_github.sh
# или
python sync_github.py
```

## 📋 Полезные команды Git

```bash
# Проверить статус (какие файлы изменены)
git status

# Посмотреть историю коммитов
git log --oneline -10

# Посмотреть изменения в файлах
git diff

# Отменить изменения в файле
git checkout -- <имя_файла>

# Временно сохранить изменения
git stash

# Вернуть сохранённые изменения
git stash pop

# Посмотреть удалённые репозитории
git remote -v

# Посмотреть все ветки
git branch -a
```

## 🆘 Решение проблем

### Ошибка: "Authentication failed"

**Причина**: Неправильный токен или истёк срок действия токена.

**Решение**:
1. Создайте новый Personal Access Token (см. Шаг 1)
2. Удалите старые credentials:
   ```bash
   # macOS:
   git credential-osxkeychain erase
   host=github.com
   protocol=https
   <нажмите Enter дважды>
   
   # Linux:
   rm ~/.git-credentials
   ```
3. Попробуйте push снова

### Ошибка: "Repository not found"

**Причина**: Репозиторий не создан на GitHub.

**Решение**: Создайте репозиторий на GitHub (см. Шаг 2)

### Ошибка: "! [rejected] main -> main (fetch first)"

**Причина**: На GitHub есть изменения, которых нет локально.

**Решение**:
```bash
git pull origin main --rebase
git push origin main
```

### Конфликты при pull

Если возникли конфликты:

```bash
# Сохранить локальные изменения
git stash

# Получить изменения с GitHub
git pull origin main

# Применить локальные изменения
git stash pop

# Разрешить конфликты вручную в файлах
# После разрешения:
git add .
git commit -m "Разрешение конфликтов"
git push origin main
```

## 🔒 Безопасность

### ⚠️ ВАЖНО:

1. **НЕ публикуйте** Personal Access Token в коде или комментариях
2. **НЕ коммитьте** файлы с секретами (`.env`, пароли, API ключи)
3. Используйте `.gitignore` для исключения чувствительных данных
4. Регулярно обновляйте токены (меняйте раз в 90 дней)
5. Если токен скомпрометирован - немедленно удалите его на GitHub и создайте новый

### Что уже исключено из Git (`.gitignore`):

- База данных (`instance/*.db`)
- Логи (`*.log`, `server.log`)
- Резервные копии (`*.tar.gz`, `backup_*/`)
- IDE настройки (`.idea/`, `.vscode/`)
- Python cache (`__pycache__/`, `*.pyc`)
- Системные файлы (`.DS_Store`)
- Загруженные файлы (`static/uploads/*`)

## 📊 Проверка успешной настройки

После выполнения всех шагов проверьте:

1. ✅ Репозиторий доступен на: https://github.com/suicidelexus/zadachnik
2. ✅ Все файлы видны на GitHub
3. ✅ Коммит виден в истории
4. ✅ Скрипты работают: `./sync_github.sh`

## 🎯 Рабочий процесс (workflow)

### На основном компьютере:

1. Вносите изменения в код
2. Запускаете: `./sync_github.sh`
3. Изменения автоматически отправляются на GitHub

### На другом компьютере:

1. Получаете изменения: `git pull origin main`
2. Работаете с кодом
3. Отправляете изменения: `./sync_github.sh`

### Синхронизация:

```
Компьютер 1  →  GitHub  →  Компьютер 2
     ↑                           ↓
     └──────────────────────────┘
```

---

## 🎉 Готово!

После выполнения этих шагов вы сможете:

✅ Синхронизировать проект между компьютерами  
✅ Сохранять историю изменений  
✅ Откатываться к предыдущим версиям  
✅ Работать из любого места  
✅ Иметь резервную копию проекта на GitHub  

**Удачи с Self's Product Board! 🚀**

Made with ❤️ for поддержания душевного баланса и равновесия 🧘

