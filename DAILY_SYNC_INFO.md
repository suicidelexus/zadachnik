# 📅 ЕЖЕДНЕВНАЯ АВТОМАТИЧЕСКАЯ СИНХРОНИЗАЦИЯ

## 🎯 Что изменилось

Автоматическая синхронизация теперь работает **один раз в сутки в 18:00**, а не каждые 30 секунд.

### ✅ Преимущества:

- **Меньше коммитов** - один коммит в день вместо множества мелких
- **Меньше нагрузки** - не постоянно работает в фоне
- **Чище история** - легче отслеживать изменения
- **Экономия трафика** - меньше операций с GitHub
- **Структурированность** - все изменения за день в одном коммите

---

## ⏰ КАК ЭТО РАБОТАЕТ

### Расписание:
- **Время синхронизации:** 18:00 (каждый день)
- **При запуске:** Делает начальную синхронизацию сразу
- **Затем:** Ждёт до 18:00 следующего дня

### Что происходит в 18:00:

1. 📥 **Pull** - получает изменения с GitHub
2. 📝 **Проверка** - смотрит есть ли локальные изменения
3. ➕ **Add** - добавляет все изменения
4. 💾 **Commit** - создаёт коммит "Daily auto-sync: 2026-02-09 18:00"
5. 📤 **Push** - отправляет на GitHub

---

## 🚀 УСТАНОВКА

### Остановите старый сервис (если запущен):

```bash
./stop_autosync.sh
```

### Запустите новый:

```bash
./setup_autosync.sh
```

**Готово!** Теперь синхронизация будет в 18:00 каждый день.

---

## 🎮 УПРАВЛЕНИЕ

### Проверить статус:
```bash
./check_autosync.sh
```

Покажет:
- Статус сервиса (запущен/остановлен)
- Время следующей синхронизации
- Последние логи

### Остановить:
```bash
./stop_autosync.sh
```

### Перезапустить:
```bash
./restart_autosync.sh
```

### Принудительная синхронизация СЕЙЧАС:
```bash
./s
```

Используйте если нужно синхронизировать срочно, не дожидаясь 18:00.

---

## 📊 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Сценарий 1: Обычный рабочий день

```bash
# Утро (09:00):
# Ничего не делаете - сервис работает в фоне

# В течение дня:
# Редактируете файлы, создаёте новые
python main.py
# Работаете как обычно

# Вечер (18:00):
# Автоматически - все изменения за день отправляются на GitHub
# Вы получаете уведомление в логах

# Вечер (после 18:00):
# Можете спокойно выключать компьютер
```

### Сценарий 2: Срочная синхронизация

```bash
# Нужно отправить изменения прямо сейчас:
./s

# Или:
python sync_github.py "Срочные изменения"
```

### Сценарий 3: Работа на двух компьютерах

**Компьютер 1 (офис):**
```bash
# Работаете весь день
# В 18:00 - автосинхронизация
```

**Компьютер 2 (дом):**
```bash
# Вечером получите изменения автоматически в 18:00
# Или принудительно:
./s
```

---

## ⚙️ НАСТРОЙКА ВРЕМЕНИ

Если хотите изменить время синхронизации:

### Вариант 1: Изменить в файле

Откройте `auto_sync_daily.py`:

```python
SYNC_TIME = "18:00"  # Измените на нужное время
```

Примеры:
- `"09:00"` - утром
- `"12:00"` - в обед
- `"21:00"` - вечером
- `"23:59"` - перед полуночью

После изменения:
```bash
./restart_autosync.sh
```

### Вариант 2: Несколько синхронизаций в день

Если нужно синхронизировать 2-3 раза в день, используйте cron вместо LaunchAgent.

Создайте cron задание:
```bash
crontab -e
```

Добавьте строки:
```
# Синхронизация в 09:00, 14:00 и 18:00
0 9 * * * cd /Users/hybrid/PycharmProjects/zadachnik && ./s >> /tmp/cron-sync.log 2>&1
0 14 * * * cd /Users/hybrid/PycharmProjects/zadachnik && ./s >> /tmp/cron-sync.log 2>&1
0 18 * * * cd /Users/hybrid/PycharmProjects/zadachnik && ./s >> /tmp/cron-sync.log 2>&1
```

---

## 📝 ЛОГИ И МОНИТОРИНГ

### Посмотреть логи в реальном времени:

```bash
tail -f /tmp/zadachnik-autosync.log
```

### Посмотреть последнюю синхронизацию:

```bash
tail -20 /tmp/zadachnik-autosync.log
```

### Проверить ошибки:

```bash
cat /tmp/zadachnik-autosync-error.log
```

### История коммитов:

```bash
git log --oneline -10
```

Вы увидите:
```
abc1234 Daily auto-sync: 2026-02-09 18:00
def5678 Daily auto-sync: 2026-02-08 18:00
ghi9012 Daily auto-sync: 2026-02-07 18:00
```

---

## 🔧 УСТРАНЕНИЕ ПРОБЛЕМ

### Проблема: "Синхронизация не происходит в 18:00"

**Проверьте:**
```bash
./check_autosync.sh
```

**Решение:**
```bash
# Перезапустите сервис:
./restart_autosync.sh

# Проверьте логи:
tail -50 /tmp/zadachnik-autosync.log
```

### Проблема: "Компьютер был выключен в 18:00"

**Что происходит:**
- При следующем включении сервис запустится автоматически
- Выполнит синхронизацию сразу
- Затем будет ждать следующих 18:00

**Ручная синхронизация:**
```bash
./s
```

### Проблема: "Нужно синхронизировать срочно"

**Решение:**
```bash
./s
```

Это не отменяет вечернюю синхронизацию в 18:00.

---

## 📋 СРАВНЕНИЕ РЕЖИМОВ

| Параметр | Каждые 30 сек | Раз в день 18:00 |
|----------|---------------|------------------|
| Частота коммитов | ~2880 в день | 1 в день |
| Нагрузка на систему | Постоянная | Минимальная |
| История Git | Очень подробная | Структурированная |
| Актуальность | Максимальная | Раз в день |
| Трафик | Высокий | Низкий |
| **Рекомендуется** | Для критичных проектов | ✅ **Для обычной работы** |

---

## 💡 РЕКОМЕНДАЦИИ

### Используйте ежедневную синхронизацию если:

✅ Работаете один или с небольшой командой  
✅ Не нужна постоянная синхронизация  
✅ Хотите структурированную историю Git  
✅ Работаете преимущественно на одном компьютере  

### Используйте частую синхронизацию (каждые 30 сек) если:

- Работаете на нескольких компьютерах одновременно
- Критически важна актуальность данных
- Команда постоянно вносит изменения

---

## 🎯 БЫСТРАЯ ШПАРГАЛКА

```bash
# Установка ежедневной синхронизации
./setup_autosync.sh

# Проверить статус
./check_autosync.sh

# Синхронизировать прямо сейчас
./s

# Остановить
./stop_autosync.sh

# Перезапустить
./restart_autosync.sh

# Посмотреть логи
tail -f /tmp/zadachnik-autosync.log

# Изменить время
nano auto_sync_daily.py  # Найти SYNC_TIME = "18:00"
./restart_autosync.sh
```

---

## 🎉 ГОТОВО!

После выполнения `./setup_autosync.sh`:

✅ Синхронизация происходит автоматически каждый день в 18:00  
✅ Сервис запускается при входе в систему  
✅ Один чистый коммит в день вместо сотен мелких  
✅ Минимальная нагрузка на систему  
✅ Если нужно срочно - используйте `./s`  

**Работайте спокойно, система сама всё синхронизирует в 18:00!** 🚀

---

Made with ❤️ for поддержания душевного баланса и равновесия 🧘

**Установлено: ежедневная синхронизация в 18:00**

