# 🤖 ПОЛНОСТЬЮ АВТОМАТИЧЕСКАЯ СИНХРОНИЗАЦИЯ С GITHUB

## 🎯 Что это делает

После настройки все изменения в проекте **автоматически** отправляются в GitHub:
- ✅ Работает в фоне постоянно
- ✅ Проверяет изменения каждые 30 секунд
- ✅ Автоматически делает commit + push
- ✅ Запускается при входе в систему
- ✅ **Вам не нужно ничего делать!**

---

## 🚀 БЫСТРАЯ УСТАНОВКА (3 команды)

### Шаг 1: Добавьте SSH ключ на GitHub (один раз)

**Ваш SSH ключ:**
```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAICuuZ6pKVObvvTEtMzLMe0XgUMwLqIIyTxPrOrj0qoWr suicidelexus@github.com
```

1. Откройте: https://github.com/settings/keys
2. Нажмите "New SSH key"
3. Title: `MacBook Air - zadachnik`
4. Key: вставьте ключ выше
5. Нажмите "Add SSH key"

### Шаг 2: Первый push (один раз)

```bash
cd /Users/hybrid/PycharmProjects/zadachnik
git push -u origin main
```

### Шаг 3: Запустите автосинхронизацию

```bash
./setup_autosync.sh
```

**ВСЁ! Теперь всё автоматически!** 🎉

---

## 🎮 УПРАВЛЕНИЕ

### Проверить статус:
```bash
./check_autosync.sh
```

### Остановить:
```bash
./stop_autosync.sh
```

### Перезапустить:
```bash
./restart_autosync.sh
```

### Посмотреть что происходит:
```bash
tail -f /tmp/zadachnik-autosync.log
```

---

## ⚙️ КАК ЭТО РАБОТАЕТ

### Технические детали:

1. **LaunchAgent (macOS сервис)**
   - Файл: `~/Library/LaunchAgents/com.zadachnik.autosync.plist`
   - Запускается автоматически при входе в систему
   - Работает постоянно в фоне

2. **auto_sync.py (Python скрипт)**
   - Проверяет изменения каждые 30 секунд
   - Если есть изменения → делает `git add .`
   - Создаёт коммит с временной меткой
   - Отправляет на GitHub через SSH

3. **SSH ключ**
   - Авторизация без пароля
   - Безопасная передача

### Что синхронизируется:

✅ Все файлы проекта (Python, HTML, CSS, JS)  
✅ Новые файлы  
✅ Изменённые файлы  
✅ Удалённые файлы  
❌ Файлы из `.gitignore` (логи, БД, кеш)

---

## 📊 СЦЕНАРИИ ИСПОЛЬЗОВАНИЯ

### Сценарий 1: Работа на одном компьютере

```bash
# Один раз:
./setup_autosync.sh

# Теперь просто работайте:
python main.py
# Редактируйте файлы
# Изменения автоматически отправляются каждые 30 сек!
```

### Сценарий 2: Работа на двух компьютерах

**Компьютер 1:**
```bash
# Настройка (один раз):
./setup_autosync.sh

# Работа:
python main.py
# Изменения автоматически синхронизируются!
```

**Компьютер 2:**
```bash
# Первый раз:
git clone git@github.com:suicidelexus/zadachnik.git
cd zadachnik
./setup_autosync.sh

# Работа:
python main.py
# Изменения автоматически синхронизируются!
```

**Переключение между компьютерами:**
- Ничего делать не нужно!
- Автосинхронизация работает на обоих компьютерах
- Изменения появляются на другом компьютере через ~30-60 секунд

### Сценарий 3: Ручная синхронизация (если нужно срочно)

```bash
./s  # Принудительная синхронизация прямо сейчас
```

---

## 🔧 НАСТРОЙКА

### Изменить интервал проверки:

Откройте `auto_sync.py` и измените:
```python
SYNC_INTERVAL = 30  # секунды
```

Варианты:
- `10` - каждые 10 секунд (очень частая синхронизация)
- `30` - каждые 30 секунд (по умолчанию)
- `60` - каждую минуту
- `300` - каждые 5 минут

После изменения:
```bash
./restart_autosync.sh
```

### Отключить автозапуск при входе:

```bash
./stop_autosync.sh
rm ~/Library/LaunchAgents/com.zadachnik.autosync.plist
```

---

## 🛠 УСТРАНЕНИЕ ПРОБЛЕМ

### Проблема: "Не могу запустить сервис"

**Решение:**
```bash
# Проверьте SSH:
ssh -T git@github.com

# Должно показать: "Hi suicidelexus!..."

# Если ошибка, добавьте ключ:
ssh-add ~/.ssh/id_ed25519
```

### Проблема: "Изменения не отправляются"

**Проверьте логи:**
```bash
tail -50 /tmp/zadachnik-autosync.log
cat /tmp/zadachnik-autosync-error.log
```

**Перезапустите:**
```bash
./restart_autosync.sh
```

### Проблема: "Конфликты при синхронизации"

Это может случиться если редактировать один файл на двух компьютерах одновременно.

**Решение:**
```bash
# Остановите автосинхронизацию:
./stop_autosync.sh

# Разрешите конфликт вручную:
git status
# Отредактируйте конфликтующие файлы
git add .
git commit -m "Разрешение конфликта"
git push

# Запустите снова:
./setup_autosync.sh
```

### Проблема: "Слишком много коммитов"

Каждое изменение создаёт отдельный коммит. Это нормально!

Если хотите "почистить" историю:
```bash
# Остановите автосинхронизацию
./stop_autosync.sh

# Объедините последние 10 коммитов в один:
git rebase -i HEAD~10
# В редакторе замените "pick" на "squash" для всех кроме первого
# Сохраните

# Отправьте:
git push --force

# Запустите автосинхронизацию:
./setup_autosync.sh
```

---

## 📋 ЧАСТО ЗАДАВАЕМЫЕ ВОПРОСЫ

### Q: Можно ли использовать на Windows/Linux?

**A:** Скрипт `setup_autosync.sh` работает только на macOS.  
Но вы можете просто запустить `python auto_sync.py` вручную в фоне.

**Linux:**
```bash
nohup python3 auto_sync.py > /tmp/autosync.log 2>&1 &
```

**Windows:**
```powershell
pythonw auto_sync.py
```

### Q: Сколько это использует трафика?

**A:** Очень мало! Git отправляет только изменения (дельты), не весь проект.  
Обычно < 1 КБ на каждый коммит.

### Q: Будет ли это замедлять компьютер?

**A:** Нет. Скрипт проверяет изменения раз в 30 секунд и почти не использует ресурсы.

### Q: Что если нет интернета?

**A:** Изменения сохраняются локально. Когда интернет появится, они автоматически отправятся.

### Q: Можно ли временно отключить?

**A:** Да:
```bash
./stop_autosync.sh    # Отключить
./setup_autosync.sh   # Включить снова
```

### Q: Синхронизируется ли база данных?

**A:** Нет, файлы БД исключены через `.gitignore`.  
Используйте экспорт/импорт через интерфейс приложения.

---

## 🎯 РЕКОМЕНДАЦИИ

### ✅ ХОРОШИЕ ПРАКТИКИ:

1. **Запускайте автосинхронизацию на всех компьютерах**
   - Изменения синхронизируются автоматически везде

2. **Проверяйте статус периодически**
   ```bash
   ./check_autosync.sh
   ```

3. **Следите за логами если что-то не так**
   ```bash
   tail -f /tmp/zadachnik-autosync.log
   ```

4. **Не редактируйте один файл одновременно на разных компьютерах**
   - Это создаст конфликты

### ⚠️ ЧЕГО ИЗБЕГАТЬ:

1. ❌ Не удаляйте `~/Library/LaunchAgents/com.zadachnik.autosync.plist` вручную
   - Используйте `./stop_autosync.sh`

2. ❌ Не запускайте `python auto_sync.py` вручную если сервис уже работает
   - Проверьте: `./check_autosync.sh`

3. ❌ Не добавляйте большие файлы (> 100 МБ) в проект
   - GitHub ограничивает размер файлов

---

## 📚 КОМАНДЫ - ШПАРГАЛКА

```bash
# УСТАНОВКА
./setup_autosync.sh       # Установить и запустить

# УПРАВЛЕНИЕ
./check_autosync.sh       # Проверить статус
./stop_autosync.sh        # Остановить
./restart_autosync.sh     # Перезапустить

# ЛОГИ
tail -f /tmp/zadachnik-autosync.log       # Смотреть в реальном времени
cat /tmp/zadachnik-autosync-error.log     # Ошибки

# РУЧНАЯ СИНХРОНИЗАЦИЯ (если нужно срочно)
./s                       # Синхронизировать сейчас

# ОТЛАДКА
ssh -T git@github.com     # Проверить SSH
git status                # Статус Git
git log --oneline -10     # Последние коммиты
```

---

## 🎉 ГОТОВО!

После выполнения `./setup_autosync.sh`:

✅ Все изменения автоматически синхронизируются  
✅ Работает при входе в систему  
✅ Работает на всех ваших компьютерах  
✅ Вам не нужно ничего делать вручную!  

**Просто работайте, и изменения сами отправляются на GitHub каждые 30 секунд!** 🚀

---

Made with ❤️ for поддержания душевного баланса и равновесия 🧘

**Работает автоматически 24/7! Настройте и забудьте!**

