# 🔄 Руководство по автоматической синхронизации с GitHub

## 📚 Доступные методы синхронизации

### 🚀 Метод 1: Ручная синхронизация (САМЫЙ ПРОСТОЙ)

**Когда использовать:** После каждого сеанса работы или когда хотите сохранить изменения.

#### Вариант A: Bash скрипт (рекомендуется)
```bash
cd /Users/hybrid/PycharmProjects/zadachnik
./sync_github.sh
```

#### Вариант B: Python скрипт
```bash
cd /Users/hybrid/PycharmProjects/zadachnik
python sync_github.py
```

#### Вариант C: Сверхбыстрая команда
```bash
cd /Users/hybrid/PycharmProjects/zadachnik
./s
```

**Что делает:**
1. ✅ Получает последние изменения с GitHub (`git pull`)
2. ✅ Добавляет все ваши изменения (`git add`)
3. ✅ Создаёт коммит с временной меткой (`git commit`)
4. ✅ Отправляет в GitHub (`git push`)

**Преимущества:**
- ✅ Полный контроль
- ✅ Видите что происходит
- ✅ Работает без доп. зависимостей

---

### 🤖 Метод 2: Авто-синхронизация в фоне (РЕКОМЕНДУЕТСЯ)

**Когда использовать:** Когда работаете долго и хотите автоматически сохранять изменения каждую минуту.

#### Запуск:
```bash
cd /Users/hybrid/PycharmProjects/zadachnik
python auto_sync.py
```

#### Остановка:
Нажмите `Ctrl+C` (скрипт предложит синхронизировать последние изменения)

**Что делает:**
- Проверяет изменения каждые 60 секунд
- Автоматически коммитит и пушит при обнаружении изменений
- Показывает статус в реальном времени

**Настройка интервала:**
Откройте `auto_sync.py` и измените:
```python
SYNC_INTERVAL = 60  # Интервал в секундах (60 = 1 минута)
```

**Преимущества:**
- ✅ Автоматическая синхронизация
- ✅ Не забудете сохранить
- ✅ Работает в фоне

**Запуск в отдельном терминале:**
```bash
# Терминал 1 (auto-sync):
cd /Users/hybrid/PycharmProjects/zadachnik
python auto_sync.py

# Терминал 2 (ваше приложение):
cd /Users/hybrid/PycharmProjects/zadachnik
python main.py
```

---

### ⚡ Метод 3: Git Hook (автопуш после каждого коммита)

**Когда использовать:** Если вы делаете коммиты вручную через Git и хотите автоматически пушить.

**Настройка уже выполнена!** Hook установлен в `.git/hooks/post-commit`

**Как работает:**
- Делаете коммит: `git commit -m "Сообщение"`
- Hook автоматически делает: `git push origin main`

**Преимущества:**
- ✅ Прозрачная работа
- ✅ Не мешает вашему workflow
- ✅ Работает только при ручных коммитах

---

## 🎯 Рекомендуемый Workflow

### На основном компьютере:

1. **Перед началом работы:**
   ```bash
   cd /Users/hybrid/PycharmProjects/zadachnik
   ./sync_github.sh  # Получить последние изменения
   ```

2. **Во время работы (выберите один вариант):**

   **Вариант А (ручная синхронизация):**
   - Работаете с кодом
   - Периодически запускаете: `./s` или `./sync_github.sh`
   
   **Вариант Б (автоматическая):**
   - В одном терминале: `python auto_sync.py`
   - В другом терминале: `python main.py`
   - Работаете с кодом - синхронизация идёт автоматически

3. **После окончания работы:**
   ```bash
   ./sync_github.sh  # Финальная синхронизация
   ```

### На другом компьютере:

1. **Перед началом работы:**
   ```bash
   cd zadachnik
   ./sync_github.sh  # Получить последние изменения
   ```

2. **Работаете аналогично основному компьютеру**

3. **После окончания работы:**
   ```bash
   ./sync_github.sh  # Отправить изменения
   ```

---

## 🔧 Детальная настройка auto_sync.py

### Изменение интервала проверки:

Откройте `auto_sync.py` и найдите:
```python
SYNC_INTERVAL = 60  # секунды
```

Примеры:
- `30` = каждые 30 секунд
- `60` = каждую минуту (по умолчанию)
- `300` = каждые 5 минут
- `600` = каждые 10 минут

### Игнорирование файлов:

В `auto_sync.py` есть список `IGNORE_PATTERNS` - файлы, которые не будут синхронизироваться.

---

## 🎬 Запуск auto_sync.py как фонового сервиса (macOS)

Если хотите, чтобы auto_sync запускался автоматически при входе в систему:

### 1. Создайте LaunchAgent:

```bash
mkdir -p ~/Library/LaunchAgents
```

### 2. Создайте файл plist:

Сохраните как `~/Library/LaunchAgents/com.zadachnik.autosync.plist`:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.zadachnik.autosync</string>
    <key>ProgramArguments</key>
    <array>
        <string>/usr/bin/python3</string>
        <string>/Users/hybrid/PycharmProjects/zadachnik/auto_sync.py</string>
    </array>
    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <false/>
    <key>StandardOutPath</key>
    <string>/tmp/zadachnik-autosync.log</string>
    <key>StandardErrorPath</key>
    <string>/tmp/zadachnik-autosync-error.log</string>
</dict>
</plist>
```

### 3. Загрузите LaunchAgent:

```bash
launchctl load ~/Library/LaunchAgents/com.zadachnik.autosync.plist
```

### 4. Управление:

```bash
# Запустить
launchctl start com.zadachnik.autosync

# Остановить
launchctl stop com.zadachnik.autosync

# Удалить из автозагрузки
launchctl unload ~/Library/LaunchAgents/com.zadachnik.autosync.plist
```

---

## 📊 Сравнение методов

| Метод | Автоматизация | Контроль | Сложность | Рекомендация |
|-------|---------------|----------|-----------|--------------|
| **Ручная синхронизация** | ⭐☆☆ | ⭐⭐⭐ | ⭐☆☆ | ✅ Для начинающих |
| **auto_sync.py** | ⭐⭐⭐ | ⭐⭐☆ | ⭐⭐☆ | ✅ Рекомендуется |
| **Git Hook** | ⭐⭐☆ | ⭐⭐⭐ | ⭐☆☆ | Для Git-пользователей |
| **LaunchAgent** | ⭐⭐⭐ | ⭐☆☆ | ⭐⭐⭐ | Для продвинутых |

---

## ❓ Частые вопросы

### Q: Что если я забуду синхронизироваться?

**A:** Используйте `auto_sync.py` - он будет синхронизировать автоматически.

### Q: Что если возникнут конфликты?

**A:** Скрипты автоматически используют `git pull --rebase`, что минимизирует конфликты. Если конфликт всё же возник:

```bash
# Посмотрите конфликтующие файлы
git status

# Отредактируйте их вручную
# Затем:
git add .
git rebase --continue
git push origin main
```

### Q: Сколько интернет-трафика это использует?

**A:** Очень мало. Git отправляет только изменения (дельты), не весь проект.

### Q: Можно ли отключить автопуш?

**A:** Да:
```bash
# Удалить Git hook:
rm .git/hooks/post-commit

# Не запускать auto_sync.py
```

### Q: Как посмотреть историю автокоммитов?

```bash
git log --oneline -20
```

### Q: Можно ли изменить сообщения коммитов?

**A:** Да, используйте:
```bash
python sync_github.py "Мое кастомное сообщение"
```

---

## 🛠 Устранение проблем

### Проблема: "Authentication failed"

**Решение:**
1. Проверьте Personal Access Token
2. Создайте новый токен: https://github.com/settings/tokens
3. Удалите старые credentials:
   ```bash
   git credential-osxkeychain erase
   host=github.com
   protocol=https
   # Нажмите Enter дважды
   ```

### Проблема: "Permission denied"

**Решение:**
```bash
chmod +x sync_github.sh
chmod +x sync_github.py
chmod +x auto_sync.py
chmod +x s
```

### Проблема: "No internet connection"

**Решение:**
- Изменения сохраняются локально
- Синхронизация произойдёт при следующей попытке
- Git буферизует коммиты

### Проблема: auto_sync.py использует много CPU

**Решение:**
Увеличьте `SYNC_INTERVAL`:
```python
SYNC_INTERVAL = 300  # 5 минут вместо 1
```

---

## 📋 Быстрая шпаргалка

```bash
# Синхронизация (быстро)
./s

# Синхронизация (обычная)
./sync_github.sh

# Синхронизация с сообщением
python sync_github.py "Добавил новую фичу"

# Автосинхронизация (в фоне)
python auto_sync.py

# Получить изменения
git pull origin main

# Проверить статус
git status

# Посмотреть историю
git log --oneline -10
```

---

## ✅ Чек-лист готовности

Перед началом работы убедитесь:

- [ ] GitHub репозиторий создан
- [ ] Personal Access Token получен
- [ ] Первый push выполнен успешно
- [ ] Скрипты исполняемые (`chmod +x`)
- [ ] `./sync_github.sh` работает
- [ ] На другом ПК можете клонировать репозиторий

**Всё готово!** 🎉

---

## 🎯 Моя рекомендация

**Для вашего сценария (работа на 2 компьютерах) рекомендую:**

1. **На каждом компьютере перед началом работы:**
   ```bash
   ./s  # Получить последние изменения
   ```

2. **Во время работы (выберите):**
   - **Вариант А**: Периодически запускайте `./s` (раз в час или когда помните)
   - **Вариант Б**: Запустите `python auto_sync.py` в отдельном терминале

3. **После окончания работы:**
   ```bash
   ./s  # Отправить все изменения
   ```

Это гарантирует, что на другом компьютере вы всегда будете продолжать с того места, где остановились! 🚀

---

Made with ❤️ for seamless development across multiple machines

